apiVersion: v1
kind: ConfigMap
metadata:
  name: causa-config
data:
  # OpenShift-specific configuration
  CAUSA_PORT: "8080"
  CAUSA_LOG_LEVEL: "INFO"
  # Environment marker
  CLUSTER_TYPE: "openshift"
